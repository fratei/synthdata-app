# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: synthdata-app
metadata:
  template: synthdata-app

services:
  api:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile

infra:
  provider: bicep
  path: ./infrastructure/bicep
